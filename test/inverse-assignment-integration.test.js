import assert from "node:assert/strict";import { readFile } from "node:fs/promises";import test from "node:test";const root=new URL("../",import.meta.url),read=path=>readFile(new URL(path,root),"utf8");const files={home:await read("index.html"),css:await read("home.css"),worker:await read("sw.js"),runtime:await read("scripts/pages-runtime-files.mjs"),smoke:await read("scripts/smoke.mjs"),workflow:await read(".github/workflows/pages.yml")};const packageJson=JSON.parse(await read("package.json"));const runtime=["antiproportionale-zuordnungen.html","inverse-assignment.css","src/inverse-assignment-app.js","src/inverse-assignment-animation.js","src/inverse-assignment-math.js","src/inverse-assignment-state.js"];
test("Startseite integriert K6.4 genau einmal als vierte Kapitel-6-Karte",()=>{const chapter=files.home.match(/<section id="zuordnungen"[\s\S]*?<\/section>/)?.[0];assert.ok(chapter);assert.equal((chapter.match(/class="module-card"/g)??[]).length,5);assert.equal((files.home.match(/href="\.\/antiproportionale-zuordnungen\.html"/g)??[]).length,1);assert.match(chapter,/Warum wird bei antiproportional aus „doppelt“ plötzlich „halb“\?/);assert.match(chapter,/Antiproportionale Zuordnungen/);assert.equal((files.home.match(/class="module-card"/g)??[]).length,32);assert.match(files.css,/\.chapter-assignments \.module-grid\s*{[^}]*repeat\(6, minmax\(0, 1fr\)\)/s);const mobile=files.css.match(/@media \(max-width: 720px\)[\s\S]*?(?=\n@media|$)/)?.[0],tablet=files.css.match(/@media \(min-width: 721px\) and \(max-width: 1040px\)[\s\S]*?(?=\n@media|$)/)?.[0],landscape=files.css.match(/@media \(orientation: landscape\) and \(min-width: 900px\) and \(max-width: 1500px\)[\s\S]*?(?=\n@media|$)/)?.[0];assert.match(mobile,/\.chapter-assignments \.module-grid\s*{[^}]*1fr/s);assert.match(tablet,/\.chapter-assignments \.module-grid\s*{[^}]*repeat\(2, minmax\(0, 1fr\)\)/s);assert.match(landscape,/\.chapter-assignments \.module-grid\s*{[^}]*repeat\(6, minmax\(0, 1fr\)\)/s);assert.match(landscape,/\.chapter-assignments \.module-card\s*{[^}]*grid-column: span 2/s);});
test("Cache v37, Pages und Smoke enthalten die sechs K6.4-Laufzeitdateien",()=>{assert.match(files.worker,/mathe-unterrichts-app-v37/);assert.doesNotMatch(files.worker,/mathe-unterrichts-app-v34/);for(const file of runtime){const pattern=new RegExp(file.replaceAll(".","\\."));assert.match(files.worker,pattern);assert.match(files.runtime,pattern);assert.match(files.smoke,pattern);}});
test("Renderer ist im Paket und Workflow genau einmal verdrahtet",()=>{assert.equal(packageJson.scripts["test:inverse-assignment-visual"],"node scripts/render-inverse-assignment-states.mjs");assert.equal((files.workflow.match(/npm run test:inverse-assignment-visual/g)??[]).length,1);});
