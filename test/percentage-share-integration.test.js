import assert from "node:assert/strict";import { readFile } from "node:fs/promises";import test from "node:test";const root=new URL("../",import.meta.url),read=path=>readFile(new URL(path,root),"utf8");const files={home:await read("index.html"),worker:await read("sw.js"),runtime:await read("scripts/pages-runtime-files.mjs"),smoke:await read("scripts/smoke.mjs"),workflow:await read(".github/workflows/pages.yml")};const packageJson=JSON.parse(await read("package.json"));const runtime=["prozent-als-anteil.html","percentage-share.css","src/percentage-share-app.js","src/percentage-share-animation.js","src/percentage-share-math.js","src/percentage-share-state.js"];
test("Startseite integriert K7.1 genau einmal als erste Kapitel-7-Karte",()=>{const chapter=files.home.match(/<section id="prozentrechnung"[\s\S]*?<\/section>/)?.[0];assert.ok(chapter);assert.equal((chapter.match(/class="module-card"/g)??[]).length,5);assert.equal((files.home.match(/href="\.\/prozent-als-anteil\.html"/g)??[]).length,1);assert.ok(chapter.indexOf("./prozent-als-anteil.html")<chapter.indexOf("./absolut-relativ.html"));assert.match(chapter,/7\. Prozentrechnung/);assert.match(chapter,/Warum sagt 25 % ohne ein Ganzes noch nicht, wie viel das ist\?/);assert.equal((files.home.match(/class="module-card"/g)??[]).length,36);assert.equal((files.home.match(/class="chapter(?:\s|")/g)??[]).length,7);});
test("Cache v41, Pages und Smoke enthalten die sechs K7.1-Laufzeitdateien",()=>{assert.match(files.worker,/mathe-unterrichts-app-v41/);assert.doesNotMatch(files.worker,/mathe-unterrichts-app-v37/);for(const file of runtime){const pattern=new RegExp(file.replaceAll(".","\\."));assert.match(files.worker,pattern);assert.match(files.runtime,pattern);assert.match(files.smoke,pattern);}});
test("Renderer ist im Paket und Workflow genau einmal verdrahtet",()=>{assert.equal(packageJson.scripts["test:percentage-share-visual"],"node scripts/render-percentage-share-states.mjs");assert.equal((files.workflow.match(/npm run test:percentage-share-visual/g)??[]).length,1);});
