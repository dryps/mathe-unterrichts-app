import assert from "node:assert/strict";import test from "node:test";import{readFile}from"node:fs/promises";const read=p=>readFile(new URL(p,import.meta.url),"utf8"),[html,css,app]=await Promise.all([read("../prozentdiagramme.html"),read("../percentage-chart.css"),read("../src/percentage-chart-app.js")]);
test("Frage, Untertitel, Beziehungen und Aha sind exakt",()=>{for(const text of ["Warum entsprechen Prozenten im Kreisdiagramm Winkel?","Prozentdiagramme","100 %","360°","1 %","3,6°","Kreisdiagramme stellen denselben relativen Anteil über einen Anteil am Vollwinkel dar."])assert.ok(html.includes(text),text);});
test("Folgerungen sind initial echt hidden",()=>{for(const id of ["pc-sector","pc-whole","pc-unit","pc-sector-relation","pc-explore","pc-conclusion"])assert.match(html,new RegExp(`id="${id}"[^>]*hidden`));assert.match(css,/\[hidden\]\{display:none!important\}/);});
test("Der Grenzfall 100 Prozent überschreibt den Verlauf mit einem echten Vollkreis",()=>{assert.match(css,/\.sector\[data-full-circle="true"\]\{background:#df7a28\}/);assert.match(app,/sector\.dataset\.fullCircle=String\(model\.sectorAngle===360\)/);});
test("Modul bleibt lokal, responsiv und offline",()=>{assert.match(html,/lokal · ohne Speicherung/);assert.doesNotMatch(html+app,/(localStorage|sessionStorage|indexedDB|fetch\(|XMLHttpRequest|https?:\/\/)/);assert.match(css,/@media\(max-width:720px\)/);assert.match(css,/@media\(min-width:721px\) and \(max-width:1040px\)/);assert.match(css,/@media\(prefers-reduced-motion:reduce\)/);assert.match(app,/serviceWorker\.register\("\.\/sw\.js", \{ scope: "\.\/", updateViaCache: "none" \}\)/);});
