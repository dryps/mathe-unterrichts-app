import assert from "node:assert/strict";import { readFile } from "node:fs/promises";import test from "node:test";const root=new URL("../",import.meta.url),read=path=>readFile(new URL(path,root),"utf8");const files={home:await read("index.html"),css:await read("home.css"),worker:await read("sw.js"),runtime:await read("scripts/pages-runtime-files.mjs"),smoke:await read("scripts/smoke.mjs"),workflow:await read(".github/workflows/pages.yml")};const packageJson=JSON.parse(await read("package.json"));const runtime=["modellwahl.html","model-choice.css","src/model-choice-app.js","src/model-choice-animation.js","src/model-choice-math.js","src/model-choice-state.js"];
test("Startseite integriert K6.5 genau einmal als fünfte Kapitel-6-Karte",()=>{const chapter=files.home.match(/<section id="zuordnungen"[\s\S]*?<\/section>/)?.[0];assert.ok(chapter);assert.equal((chapter.match(/class="module-card"/g)??[]).length,5);assert.equal((files.home.match(/href="\.\/modellwahl\.html"/g)??[]).length,1);assert.match(chapter,/Warum muss ich vor dem Dreisatz wissen, welches Modell vorliegt\?/);assert.match(chapter,/Proportional oder antiproportional\?/);assert.equal((files.home.match(/class="module-card"/g)??[]).length,40);assert.match(files.css,/\.chapter-assignments \.module-grid\s*{[^}]*repeat\(6, minmax\(0, 1fr\)\)/s);assert.match(files.css,/\.chapter-assignments \.module-card\s*{[^}]*grid-column:\s*span 2/s);const mobile=files.css.match(/@media \(max-width: 720px\)[\s\S]*?(?=\n@media|$)/)?.[0],tablet=files.css.match(/@media \(min-width: 721px\) and \(max-width: 1040px\)[\s\S]*?(?=\n@media|$)/)?.[0],landscape=files.css.match(/@media \(orientation: landscape\) and \(min-width: 900px\) and \(max-width: 1500px\)[\s\S]*?(?=\n@media|$)/)?.[0];assert.match(mobile,/\.chapter-assignments \.module-card\s*{[^}]*grid-column:\s*auto/s);assert.match(tablet,/\.chapter-assignments \.module-grid\s*{[^}]*repeat\(2, minmax\(0, 1fr\)\)/s);assert.match(landscape,/\.chapter-assignments \.module-grid\s*{[^}]*repeat\(6, minmax\(0, 1fr\)\)/s);});
test("Cache v45, Pages und Smoke enthalten die sechs K6.5-Laufzeitdateien",()=>{assert.match(files.worker,/mathe-unterrichts-app-v45/);assert.doesNotMatch(files.worker,/mathe-unterrichts-app-v35/);for(const file of runtime){const pattern=new RegExp(file.replaceAll(".","\\."));assert.match(files.worker,pattern);assert.match(files.runtime,pattern);assert.match(files.smoke,pattern);}});
test("Renderer ist im Paket und Workflow genau einmal verdrahtet",()=>{assert.equal(packageJson.scripts["test:model-choice-visual"],"node scripts/render-model-choice-states.mjs");assert.equal((files.workflow.match(/npm run test:model-choice-visual/g)??[]).length,1);});
