import assert from "node:assert/strict";import test from "node:test";import { PERCENTAGE_SHARE_REVEAL_DURATION,percentageShareRevealFrame } from "../src/percentage-share-animation.js";
test("Reveal blendet exakt und weist nicht endliche Zeiten ab",()=>{assert.deepEqual(percentageShareRevealFrame(0),{opacity:0,complete:false});assert.deepEqual(percentageShareRevealFrame(PERCENTAGE_SHARE_REVEAL_DURATION/2),{opacity:0.5,complete:false});assert.deepEqual(percentageShareRevealFrame(PERCENTAGE_SHARE_REVEAL_DURATION),{opacity:1,complete:true});assert.throws(()=>percentageShareRevealFrame(Number.NaN),/endlich/);});
