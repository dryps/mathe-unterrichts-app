import { PERCENTAGE_WHOLES,percentageShare,snapPercentageWhole } from "./percentage-share-math.js";export const PERCENTAGE_SHARE_VIEWS=Object.freeze({irritation:"irritation",hundred:"hundred",twenty:"twenty",eighty:"eighty",twoHundred:"twoHundred",explore:"explore"});const ORDER=Object.freeze(Object.values(PERCENTAGE_SHARE_VIEWS)),CONCLUSION="Prozent ist relativ, keine feste absolute Anzahl.";const make=(view,locked=false,wholeIndex=0)=>Object.freeze({view,locked:Boolean(locked),wholeIndex:Number(wholeIndex)});export function createPercentageShareState(){return make(PERCENTAGE_SHARE_VIEWS.irritation);}export function nextPercentageShareState(current){if(current.locked)return current;const index=ORDER.indexOf(current.view);if(index<0||index>=ORDER.length-1)return current;return make(ORDER[index+1],true,current.wholeIndex);}export function finishPercentageShareReveal(current){return current.locked?make(current.view,false,current.wholeIndex):current;}export function setPercentageWhole(current,index){if(current.locked||current.view!==PERCENTAGE_SHARE_VIEWS.explore)return current;const whole=snapPercentageWhole(Number(index));return make(current.view,false,PERCENTAGE_WHOLES.indexOf(whole));}export function resetPercentageShareState(){return createPercentageShareState();}export function percentageShareViewModel(current){const rank=ORDER.indexOf(current.view);if(rank<0)throw new RangeError("Unbekannter Lernzustand.");const whole=snapPercentageWhole(current.wholeIndex),record=percentageShare(whole),equation=`25 % von ${whole} = ${record.part}`,output=`Ganzes ${whole} → Anteil ${record.part}`;const insights={irritation:"25 % wovon? Ohne das Ganze bleibt die Anzahl offen.",hundred:"25 % bedeutet zunächst 25 von 100 gleich großen Teilen.",twenty:"Bei einem Ganzen von 20 sind 25 % genau 5.",eighty:"Bei einem Ganzen von 80 sind 25 % genau 20.",twoHundred:"Bei einem Ganzen von 200 sind 25 % genau 50.",explore:CONCLUSION};return Object.freeze({view:current.view,whole,part:record.part,wholeIndex:PERCENTAGE_WHOLES.indexOf(whole),equation,output,sliderAriaLabel:`Ganzes erkunden: ${whole}`,sliderValueText:`Ganzes ${whole}, davon 25 Prozent gleich ${record.part}`,trackAriaLabel:`${record.part} von ${whole} sind markiert; das sind 25 Prozent.`,showHundred:rank>=1,showTwenty:rank>=2,showEighty:rank>=3,showTwoHundred:rank>=4,showExplore:rank>=5,showConclusion:rank>=5,showNext:rank<5,controlsInteractive:rank>=5&&!current.locked,conclusion:CONCLUSION,insight:insights[current.view],liveText:rank>=5?`${equation}. ${CONCLUSION}`:insights[current.view]});}