import { dreisatzPath,formatEuro,snapTarget } from "./proportional-rule-three-math.js";export const RULE_THREE_VIEWS=Object.freeze({irritation:"irritation",divide:"divide",unit:"unit",multiply:"multiply",result:"result",explore:"explore"});const ORDER=Object.freeze(Object.values(RULE_THREE_VIEWS)),CONCLUSION="Die Einheit 1 ist gemeinsame Referenz der proportionalen Skalierung.";const make=(view,locked=false,target=8)=>Object.freeze({view,locked:Boolean(locked),target:Number(target)});export function createRuleThreeState(){return make(RULE_THREE_VIEWS.irritation);}export function nextRuleThreeState(current){if(current.locked)return current;const index=ORDER.indexOf(current.view);if(index<0||index>=ORDER.length-1)return current;return make(ORDER[index+1],true,current.target);}export function finishRuleThreeReveal(current){return current.locked?make(current.view,false,current.target):current;}export function setRuleThreeTarget(current,target){if(current.locked||current.view!==RULE_THREE_VIEWS.explore)return current;return make(current.view,false,snapTarget(Number(target)));}export function resetRuleThreeState(){return createRuleThreeState();}
export function ruleThreeViewModel(current){const rank=ORDER.indexOf(current.view);if(rank<0)throw new RangeError("Unbekannter Lernzustand.");const path=dreisatzPath(current.target),targetQuantityText=`${path.targetQuantity} Stück`,targetPriceText=formatEuro(path.targetCents),multiplyText=`× ${path.multiplyBy}`,resultText=`${targetQuantityText} → ${targetPriceText}`;const insights={irritation:"Von 3 Stück zu 8 Stück gibt es keinen direkten ganzen Faktor.",divide:"Beide Größen werden durch 3 geteilt.",unit:"Der Wert für 1 ist jetzt die gemeinsame Referenz.",multiply:"Beide Größen werden mit demselben Zielwert multipliziert.",result:"So bleiben Menge und Preis proportional skaliert.",explore:CONCLUSION};return Object.freeze({view:current.view,path,targetQuantityText,targetPriceText,multiplyText,resultText,showDivide:rank>=1,showUnit:rank>=2,showMultiply:rank>=3,showResult:rank>=4,showExplore:rank>=5,showConclusion:rank>=5,showNext:rank<5,controlsInteractive:rank>=5&&!current.locked,conclusion:CONCLUSION,insight:insights[current.view],liveText:rank>=5?`${resultText}. ${CONCLUSION}`:insights[current.view]});}
