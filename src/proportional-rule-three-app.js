import { RULE_THREE_REVEAL_DURATION,ruleThreeRevealFrame } from "./proportional-rule-three-animation.js";import { createRuleThreeState,finishRuleThreeReveal,nextRuleThreeState,resetRuleThreeState,ruleThreeViewModel,setRuleThreeTarget } from "./proportional-rule-three-state.js";const $=selector=>document.querySelector(selector),workspace=$("#rule-three-workspace"),divideQuantity=$("#rule-three-divide"),dividePrice=$("#rule-three-divide-price"),unitQuantity=$("#rule-three-unit"),unitPrice=$("#rule-three-unit-price"),unitExample=$("#rule-three-unit-example"),multiplyQuantityStage=$("#rule-three-multiply"),multiplyPriceStage=$("#rule-three-multiply-price"),multiplyQuantity=$("#multiply-quantity"),multiplyPrice=$("#multiply-price"),resultQuantity=$("#rule-three-result"),resultPrice=$("#rule-three-result-price"),resultExample=$("#rule-three-result-example"),targetQuantity=$("#target-quantity"),targetPrice=$("#target-price"),explore=$("#rule-three-explore"),conclusion=$("#rule-three-conclusion"),targetInput=$("#rule-three-target"),output=$("#rule-three-output"),insight=$("#rule-three-insight"),live=$("#rule-three-live"),next=$("#rule-three-next"),reset=$("#rule-three-reset");let current=createRuleThreeState(),frameId=null,timerId=null,animationToken=0;
function clearAnimation(){animationToken += 1;if(frameId!==null)cancelAnimationFrame(frameId);if(timerId!==null)clearTimeout(timerId);frameId=null;timerId=null;next.disabled=false;workspace.style.opacity="1";workspace.setAttribute("aria-busy","false");}function render(){const model=ruleThreeViewModel(current);workspace.dataset.state=model.view;divideQuantity.hidden=!model.showDivide;dividePrice.hidden=!model.showDivide;unitQuantity.hidden=!model.showUnit;unitPrice.hidden=!model.showUnit;unitExample.hidden=!model.showUnit;multiplyQuantityStage.hidden=!model.showMultiply;multiplyPriceStage.hidden=!model.showMultiply;resultQuantity.hidden=!model.showResult;resultPrice.hidden=!model.showResult;resultExample.hidden=!model.showResult;explore.hidden=!model.showExplore;conclusion.hidden=!model.showConclusion;targetQuantity.textContent=model.targetQuantityText;targetPrice.textContent=model.targetPriceText;multiplyQuantity.textContent=model.multiplyText;multiplyPrice.textContent=model.multiplyText;resultExample.textContent=model.resultText;targetInput.value=String(model.path.targetQuantity);targetInput.disabled=!model.controlsInteractive;output.textContent=model.resultText;insight.textContent=model.insight;next.hidden=!model.showNext;next.disabled=current.locked;live.textContent=model.liveText;}
function finishReveal(token){if(token!==animationToken||!current.locked)return;clearAnimation();current=finishRuleThreeReveal(current);render();}function animateReveal(){clearAnimation();const token=animationToken;workspace.style.opacity="0";workspace.setAttribute("aria-busy","true");next.disabled=true;if(window.matchMedia("(prefers-reduced-motion: reduce)").matches){finishReveal(token);return;}let started=null;timerId=setTimeout(()=>finishReveal(token),RULE_THREE_REVEAL_DURATION+120);function animate(time){if(token!==animationToken||!current.locked)return;if(started===null)started=time;const frame=ruleThreeRevealFrame(time-started);workspace.style.opacity=String(frame.opacity);if(frame.complete)finishReveal(token);else frameId=requestAnimationFrame(animate);}frameId=requestAnimationFrame(animate);}next.addEventListener("click",()=>{if(current.locked)return;current=nextRuleThreeState(current);render();if(current.locked)animateReveal();});reset.addEventListener("click",()=>{clearAnimation();current=resetRuleThreeState();render();});targetInput.addEventListener("input",event=>{current=setRuleThreeTarget(current,Number(event.currentTarget.value));render();});if("serviceWorker" in navigator)window.addEventListener("load",()=>navigator.serviceWorker.register("./sw.js", { scope: "./", updateViaCache: "none" }).catch(()=>{}));render();
