export function snapTarget(value){if(!Number.isFinite(value))throw new RangeError("Der Zielwert muss endlich sein.");return Math.max(2,Math.min(10,Math.round(value)));}
export function formatEuro(cents){if(!Number.isInteger(cents)||cents<0)throw new RangeError("Der Centwert muss eine nicht negative ganze Zahl sein.");const euros=Math.floor(cents/100),rest=String(cents%100).padStart(2,"0");return cents%100===0?`${euros} €`:`${euros},${rest} €`;}
export function dreisatzPath(target){const targetQuantity=snapTarget(target),startQuantity=3,startCents=750,divideBy=3,unitQuantity=startQuantity/divideBy,unitCents=startCents/divideBy,multiplyBy=targetQuantity,targetCents=unitCents*multiplyBy;return Object.freeze({startQuantity,startCents,divideBy,unitQuantity,unitCents,multiplyBy,targetQuantity,targetCents});}
