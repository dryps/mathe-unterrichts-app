import { createFixedParallelogram, createMovableParallelogram, vertexAngle } from "./unique-quadrilateral-math.js";

export const UNIQUE_QUADRILATERAL_VIEWS=Object.freeze({irritation:"irritation",insufficient:"insufficient",dependent:"dependent",independent:"independent"});
const ORDER=Object.freeze(Object.values(UNIQUE_QUADRILATERAL_VIEWS));
const CONCLUSION="Nicht die Anzahl, sondern die Unabhängigkeit der Angaben legt die Figur fest.";
const make=(view,locked=false,shear=0)=>Object.freeze({view,locked:Boolean(locked),shear:Number(shear)});
export function createUniqueQuadrilateralState(){return make(UNIQUE_QUADRILATERAL_VIEWS.irritation);}
export function nextUniqueQuadrilateralState(current){if(current.locked)return current;const index=ORDER.indexOf(current.view);if(index<0||index>=ORDER.length-1)return current;return make(ORDER[index+1],true,current.shear);}
export function finishUniqueQuadrilateralReveal(current){return current.locked?make(current.view,false,current.shear):current;}
export function setQuadrilateralShear(current,shear){if(current.locked||![UNIQUE_QUADRILATERAL_VIEWS.insufficient,UNIQUE_QUADRILATERAL_VIEWS.dependent].includes(current.view))return current;createMovableParallelogram(Number(shear));return make(current.view,false,shear);}
export function resetUniqueQuadrilateralState(){return createUniqueQuadrilateralState();}
export function uniqueQuadrilateralViewModel(current){const rank=ORDER.indexOf(current.view);if(rank<0)throw new RangeError("Unbekannter Lernzustand.");const fixed=current.view===UNIQUE_QUADRILATERAL_VIEWS.independent;const geometry=fixed?createFixedParallelogram():createMovableParallelogram(current.shear);const angle=Math.round(vertexAngle(geometry.points,0));const insights={irritation:"Welche Angaben verhindern, dass sich dieses Viereck noch verändert?",insufficient:"Gegenüberliegende Seiten sind parallel – doch die Figur bleibt beweglich.",dependent:"Gleich lange Gegenseiten folgen bereits. Der Freiheitsgrad bleibt offen.",independent:"Grundseite, Nachbarseite und eingeschlossener Winkel schließen den Freiheitsgrad."};const boardDescription=rank===0?"Unmarkiertes konvexes Viereck.":rank===1?`Bewegliches Parallelogramm mit einem Winkel von etwa ${angle} Grad.`:rank===2?`Bewegliches Parallelogramm: Gegenseiten sind parallel und gleich lang; Winkel etwa ${angle} Grad.`:"Festgelegtes Parallelogramm mit AB 6 Zentimeter, AD 4 Zentimeter und Winkel DAB 70 Grad.";return Object.freeze({view:current.view,points:geometry.points,showParallel:rank>=1,showEqual:rank>=2,showFreedom:rank===1||rank===2,showIndependent:rank>=3,showConclusion:rank>=3,showNext:rank<3,controlsInteractive:(rank===1||rank===2)&&!current.locked,dimensionText:"AB = 6 cm · AD = 4 cm · ∠DAB = 70°",conclusion:CONCLUSION,scopeNote:"Für dieses Parallelogramm ist die Form bis auf kongruente Lage festgelegt.",insight:insights[current.view],boardDescription,liveText:rank>=3?`${CONCLUSION} ${"Für dieses Parallelogramm ist die Form festgelegt."}`:insights[current.view]});}
